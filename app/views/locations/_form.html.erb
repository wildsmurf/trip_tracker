<%= form_for @location do |f| %>
  <div class="field">  
    <%= f.label :name %><br>
    <div class="ui input focus">
      <%= f.text_field :name, required: true, autofocus: true %>
    </div>
  </div>
  <br>
  <%= f.submit 'Add New Destination', class: "ui positive button"%>
<% end %>