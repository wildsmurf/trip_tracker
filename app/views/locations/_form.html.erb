<h1><%= @location.id ? 'Edit Location Name' : 'Add Location' %></h1>

<%= form_for @location do |f| %>
<%= f.label :name %>
<%= f.text_field(:name, required: true, autofocus: true) %>
<%= f.submit %>
<% end %>