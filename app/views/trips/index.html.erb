<h1>Welcome Trip Planner</h1>
<p>Here you can create a <%= link_to 'New Trip', new_trip_path %>, or browse all Destinations</p>

<ul>
  <% @trips.each do |trip| %>
    <li><h3><%= link_to trip.name, trip_path(trip.id) %></h3></li><%= link_to 'Delete Trip', trip, method: :delete, data: { confirm: 'Are you sure?' } %>
    <br>
  <% end %>
</ul>


<br>
